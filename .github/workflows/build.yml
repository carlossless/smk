name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    strategy:
      matrix:
        keyboard: [example, eyooso-z11, nuphy-air60]
        nkro: [enabled, disabled]
        buildtype: [debug, release]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - uses: cachix/install-nix-action@v31
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - uses: cachix/cachix-action@v17
      with:
        name: smk
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
    - name: Setup
      run: nix develop --command meson setup build --cross-file cross-file/${{ matrix.keyboard }}.ini -Dnkro=${{ matrix.nkro }} -Dbuildtype=${{ matrix.buildtype }}
    - name: Build
      run: nix develop --command meson compile -C build
    - name: Archive code coverage results
      uses: actions/upload-artifact@v7
      with:
        name: smk-${{ matrix.keyboard }}-${{ matrix.buildtype }}-${{ matrix.nkro == 'enabled' && 'nkro' || '6kro' }}
        path: build/*.hex

  sim-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - uses: cachix/install-nix-action@v31
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - uses: cachix/cachix-action@v17
      with:
        name: smk
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
    - name: Setup
      run: nix develop --command meson setup build --cross-file cross-file/nuphy-air60.ini
    - name: Build firmware (nuphy-air60)
      run: nix develop --command meson compile -C build
    - name: Run simulator tests
      run: nix develop --command meson test -C build sim-tests --print-errorlogs
    - name: Upload meson testlog on failure
      if: failure()
      uses: actions/upload-artifact@v7
      with:
        name: sim-tests-testlog
        path: build/meson-logs/testlog.txt

  format-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - uses: cachix/install-nix-action@v31
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - uses: cachix/cachix-action@v17
      with:
        name: smk
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
    - name: Setup
      run: nix develop --command meson setup build
    - name: Lint
      run: nix develop --command ninja -C build clang-format-check

